---
name: Blog API reminder — Island Cabinets & Floors
description: User needs to finish other pages before wiring up the blog API; remind them when they return to the Island Cabinets project.
type: project
---

The blog page (blog.html) uses static placeholder post cards. The user plans to replace these with live posts pulled from an API, but doesn't yet know which platform they're using or have the API key.

**What's needed before I can write the fetch code:**
- Which CMS/platform hosts the blog (GoHighLevel/HyppoAI, WordPress, Ghost, headless CMS, etc.)
- The API endpoint URL
- The API key / token (read-only public key preferred for a static site)
- The JSON response shape (fields: title, excerpt, date, slug, featuredImage, category, etc.)
- Whether CORS is enabled on the API for their domain

**How to apply:** When the user returns to this project and says they've finished other pages, or asks about the blog, remind them: "You wanted to come back to wiring up the blog API — do you have the platform/API details now?"
